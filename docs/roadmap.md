# Feature roadmap and current priority

## Priority decision — 2026-09-07

The user explicitly authorized forward feature development from the current working implementation, superseding the earlier requirement to resolve historical movement failures before starting another milestone.

**Outstanding movement issues are DEFERRED KNOWN LIMITATIONS; their engineering status remains UNRESOLVED. They do not automatically block authorized feature development, with Milestone 16 accepted for continued feature development with deferred movement validation and Milestone 12 accepted under the separate interface exception below. Full movement acceptance and full Milestone 5 regression acceptance have not passed.** This is a scheduling decision, not a bug closure, test waiver or claim of movement reliability. A full movement-stress pass is not an automatic prerequisite for every new feature; demonstrated feature regressions and separate runtime defects are not covered by this deferral.

This decision supersedes earlier instructions in milestone/investigation reports that all subsequent work must remain blocked or that another movement investigation must come next. Those reports retain their historical results, failures, reproduction constraints and evidence. Closed historical execution budgets are not reopened.

- Preserve the current implementation, including Issue A's parked-neighbor repair, the projection step-size repair, the bounded local escape repair and the numerical gate-corner repair. Do not roll back validated repairs.
- Preserve existing tests and assertions, fixtures, failed executions, reproduction records, diagnostics and retained evidence, including ignored `validation-output/` files.
- Keep open cases visible in [movement issue records](movement-issue-records.md). Do not start movement investigation, historical replay, recorder expansion or repair unless the user requests it or concrete evidence demonstrates that it directly prevents required next-milestone behavior. Scope any necessary work to that demonstrated dependency.
- Continue relevant regression testing and report all failures. A passing rerun does not erase a failure; a familiar assertion name does not establish that a new failure is pre-existing. Use retained baseline/source evidence for attribution, and label uncertain attribution unknown.
- Assess each feature milestone separately from deferred movement acceptance. The deferral does not excuse new regressions or failure of that milestone's own requirements.

## Milestone 12 acceptance decision — 2026-09-09

**Milestone 12: accepted for continued feature development with a documented interface exception.** The owner explicitly supersedes the requirement to establish and correct the unattributed historical group 3 assignment/recall failure before moving beyond M12. The development gate is closed by project acceptance; the [interface issue](movement-issue-records.md#m12--historical-group-3-assignmentrecall-failure-open) remains **OPEN**, its cause **UNKNOWN**, and criterion 7 **NOT VERIFIED**. The 1,030 passing compatibility checks across six source-matched executions are current tested evidence, not proof that the original failure was repaired.

This exception applies only to that historical occurrence. Preserve control-group tests and assertions, ownership, selection and keyboard-focus requirements, builder results, failed executions, diagnostics and the new failure-only snapshots. A demonstrated new interface regression must still be addressed; unrelated failures are not automatically accepted. Separately deferred movement findings remain unresolved, and neither complete movement nor historical regression acceptance is claimed. No human keyboard-and-mouse playtest has occurred.

Revisit the issue when normal play or relevant testing supplies actionable evidence, or when a change to the affected interface requires investigating a reproduced failure. This documentation-only decision launches no historical replay, diagnostic campaign, broad review or Godot run. The [M12 report](milestone-12.md#owner-authorized-interface-exception--2026-09-09) records the scope and preserved evidence. That closeout preceded the now-implemented Milestone 13 power extension below; its historical acceptance decision is unchanged.

## Implemented sequence and current feature

At the priority decision, the repository documented milestones through Milestone 5 and its corrective work, with no subsequent feature defined. Milestone 6 was then recommended and explicitly authorized for implementation; its current behavior and evidence are in [the Milestone 6 report](milestone-6.md).

| Milestone | Feature / status |
| --- | --- |
| 1–1.5.1 | Camera, selection, group movement and crowd controls; validated repairs retained, movement limitations remain |
| 2–2.5.1 | Health, Rifle/Rocket combat, pursuit/retaliation, line of fire and spherical projectile collision |
| 3 | Fixed HQ/barracks, shared credits, Rifle queues and rally points |
| 4 | Finite supply harvesting, collector cargo and automatic deposits |
| 5 | Player-placed barracks, paid timed construction, cancellation and serialized navigation updates; feature implemented, full regression acceptance not passed |
| **6 — implemented** | **Playable base assault: damageable HQs/completed barracks, an integrated economy-to-combat scenario, victory/defeat/draw and restart. See the separate feature validation report.** |
| **7 — implemented; feature checks pass** | **Vehicle Factory and existing Rocket Vehicle production, through shared construction/queues, in a combined-arms assault variant. All A–G feature groups pass automated headless/graphical validation, including the zero-credit earned-army victory loop. Movement acceptance remains separate; see [results and limitations](milestone-7.md).** |
| **8 / 8.1 — complete** | **Fully revealed minimap, groups 1–9, compact Help and contextual HUD in the combined-arms scene. All eight final acceptance groups pass in [Milestone 8.1](milestone-8.1.md); the earlier [M8 report](milestone-8.md) retains its historical scaffold.** |
| **9 — attack-move; complete** | **All eight acceptance groups PASS. Q/button battlefield or minimap targeting for existing Rifles/Rockets, bounded automatic engagements and resumed original slots through existing movement/combat. Expanded/collapsed Help verified at 1280x720 and 1920x1080. See [Milestone 9](milestone-9.md).** |
| **10 — enemy economy and repeat assaults; complete** | **All eight feature groups PASS in the separate economy-assault scene. Real harvesting funds paid Rifle production, staging and existing attack-move waves. Opponent/integration: 261 passing assertions in both modes. Full matrix: 59/60 executions, 7,393 assertions, three graphical movement-stress failures of unknown attribution, exit 1. See [Milestone 10](milestone-10.md).** |
| **11 — Supply Depots and Collector production; complete** | **All eight feature groups PASS in Supply Depot Assault. Depot checks 272/284 and earned integration 80/81 pass both modes. Fresh-copy full matrix: 63/64 executions, 8,110 checks, three headless projection-fixture failures of unknown attribution, exit 1. See [Milestone 11](milestone-11.md).** |
| **12 — Bulldozers and builder-driven construction; accepted with a documented interface exception** | Accepted for continued feature development by the owner. Original builder mechanics and earned opening passed 710 checks in four matrix executions. Original matrix finished 65/68 passing, 8814 checks / 14 failures, exit 1. Criterion 7 remains NOT VERIFIED after the unknown-cause historical group 3 assignment/recall failure; 1,030 passing compatibility checks across six executions do not close it. Both movement-stress failures remain separately deferred. See [final M12 handoff](milestone-12.md). |
| **13 — Power generation; COMPLETE** | All eight acceptance groups PASS in [Power Assault](../scenes/power_assault.tscn): builder-built 500-credit / 10-second / 10-power plants, per-owner demand, 50% shortage production, HUD and lifecycle/Restart. Fresh-copy matrix: 74/74 executions, 9,711 checks, zero failures/native errors, exit 0. Stable-ID continuity correction is test-only. Earlier scenes opt out; historical exceptions stay open. [M13 report](milestone-13.md). |
| **14 — Powered Ground Defense Battery; COMPLETE** | All eight feature groups PASS within the preserved M12 interface exception in [Defense Assault](../scenes/defense_assault.tscn). Bulldozer-built 700-credit / 12-second / 600-HP battery, 3 nominal demand, 18 hitscan damage / 0.75-second cooldown / 12-unit range. Eight defense executions pass 1,040 checks. Fresh-copy matrix: 82/82 completed, 81 passing, 10,751 checks, five graphical movement-stress failures of UNKNOWN attribution, exit 1. [Completed M14 report](milestone-14.md). |
| **16 — Walls, manual gates and basic breaching; accepted for continued feature development with deferred movement validation** | [Fortified Assault](../scenes/fortified_assault.tscn) is playable. Original matrix: 102/102 completed, 99 passing, 12,829 checks, five failures, three native error lines, exit 1; includes all 1,073 passing M16 checks. The three error lines report the three movement assertions, with no separate runtime error. Preview-fixture correction passes 545 separate targeted checks, exit 0. Graphical choke_50 attribution remains UNKNOWN: group 7 NOT VERIFIED; groups 1–6 and 8 PASS. [M16 handoff](milestone-16.md). |

Milestone 6 gives construction, harvesting, production and combat a shared match objective. It uses a small flat map and existing units, resources and commands. Strategic AI, additional economic systems and general movement reliability work remain outside this milestone.

Milestone 6's original scope and validation remain in its [implementation prompt](milestone-6-prompt.md) and [report](milestone-6.md). Milestone 7 added one factory, one recipe for the existing Rocket Vehicle and a combined-arms scene reusing the base-assault lifecycle. [Milestone 7](milestone-7.md) records its completed implementation and historical validation separately from deferred movement acceptance.

The completed **Milestone 8 — tactical minimap and control groups** remains in `res://scenes/combined_arms_assault.tscn`. Left-click the minimap to center the existing camera; right-click for ordinary ground Move. **Ctrl + 1–9** replaces/clears a mobile-unit group, **1–9** recalls it, and a double-tap centers the camera. Marker refresh defaults to **0.1 seconds**, and double-tap timing to **0.3 seconds** of real input time. The map uses fixed +X-right/+Z-down orientation and shows the fully revealed field. Existing selection, ownership, command acceptance, construction, production and match-result/Restart systems remain authoritative.

Final M8/8.1 acceptance is saved in [Milestone 8.1](milestone-8.1.md): 18 post-fix executions / 3,146 passing checks plus four unaffected combat/line-of-fire executions / 987 reused passing checks, with 12 corrected-source captures inspected. This supersedes the initial M8 report's pending acceptance entries. No human playtest occurred.

**Milestone 9 — Attack Move commands is COMPLETE (2026-09-08)** in the same playable scene. Q or the contextual button enters ground targeting; battlefield and minimap clicks share normal batch-command authority. Existing armed units retain an original final slot, acquire nearby eligible hostiles with shared line-of-fire geometry, pursue/fire using existing combat, abandon bounded engagements and resume travel. Collectors remain ineligible and keep their work in mixed selections. Ordinary Move/Attack/Stop and group recall preserve their separate meanings.

The [M9 report](milestone-9.md) records the original complete matrix (**56 executions / 7,116 checks**) and final source coverage: **50 reused executions / 6,277 checks + six post-layout executions / 855 checks**, all passing with zero failures/native error lines and exits 0. The affected reruns cover HUD, attack-move UI and tactical interface in both modes, including produced Rifle/Rocket damage, independent physical resumed travel and retained distinct slot arrival. Source hashes, corrected Help captures, earlier failed executions and all eight PASS acceptance groups are saved. No human playtest occurred.

The completed M9 scenes retain their starting forces, credits, costs, resource amounts, production timing, 90-second scripted enemy assault and result/Restart rules.

**Milestone 10 — enemy economy and repeat assaults is COMPLETE (2026-09-08)** in the separate `res://scenes/economy_assault.tscn`. Open it and press F6, or use the installed Godot with `--path . res://scenes/economy_assault.tscn`. A bounded scene-local controller uses real owner-isolated harvesting, paid Rifle queues, assembled produced troops and existing attack-move. Defaults remain 300 enemy credits, a new finite 2000-supply cache, 0.5-second planning, 90-second earliest wave, 60-second launch interval, preferred/maximum three recipients, 30-second partial wait and a 12-unit combat population including pending deployments. The first threshold is earliest eligibility; production and staging can delay launch. Earlier playable scenes retain their one-shot assaults.

The [M10 report](milestone-10.md) saves all eight PASS feature groups, actual commands/exits, original failed runs and normal captures at 1280x720 and 1920x1080. The completed, source-matched matrix has **60/60 completed executions, 59 passing, 7,393 assertions, three failures and phase exit 1**. Opponent checks pass **96 headless / 98 graphical**, and zero-credit integration passes **33 / 34**, including real damage by every member of two paid waves and exact deposit/spending reconciliation. These 261 assertions are part of the matrix, not an additional acceptance total. No runtime changes or engine reruns were needed during final recovery; only the three handoff documents changed.

**Unknown attribution:** graphical `movement_stress_checks` failed three `choke_30` arrival/traversal/settling assertions for unit 13. This occurrence is not established as pre-existing or newly introduced. Its failed log and metrics remain linked in M10, without retries or historical movement investigation. No M10 feature failures or demonstrated newly introduced regressions remain; documented deferred movement findings remain separately unresolved. Feature completion does not claim a clean full regression matrix or a human playtest.

No strategic planner, rebuilds, free reinforcement income, fog of war, patrol/guard, stances, multiplayer, automatic flanking or movement investigation is included in the completed M10 scope.

**Milestone 11 — Supply Depots and Collector production is COMPLETE.** Open `scenes/supply_depot_assault.tscn` in the installed Godot 4.7.2 and press F6, or launch with `--path . res://scenes/supply_depot_assault.tscn`. The prototype HQ builds a depot for 300 credits / ten simulated seconds; its completed 450-HP building trains ordinary unarmed Collectors for 200 credits / six seconds. Automatic deliveries choose the shortest usable owned HQ/depot route; explicit manual returns deliver once to the selected building. Deposits, finite resources, shared wallets, cancellation/refunds, safe deployment, enemy economy, minimap, contextual HUD and match-result/Restart remain integrated.

The [M11 report](milestone-11.md) saves the real zero-credit depot-to-collector-to-Rifle ledger and corrected return fixture. Ordinary loaded movement makes both measured return origins comparable; the depot's 3.294-unit route delivers in 1.783 seconds versus HQ's 7.345 units / 2.783 seconds including unchanged unloading. This establishes the tested clear-route advantage, not universal income under congestion. Final fresh-copy import passes and the single expanded matrix completes **64/64 executions, 63 passing, 8,110 checks, three failures, phase exit 1**. All four M11 executions pass **717 checks**, and all enemy economy/lifecycle/UI compatibility suites pass. No human playtest occurred.

**M11 unknown attribution:** headless `projection_step_checks` reports unit 41 FAILED after eight recoveries, producing all-participant arrival, settling/separation and unit-41 arrival failures. Its relationship to M11 changes or historical movement cases is unestablished. The graphical counterpart and both movement-stress modes pass. The failure remains saved without a clean-run retry; the previous M10 graphical stress failure and documented deferred movement findings also remain unresolved.

## Next focused feature — builder-driven construction

**Milestone 12 — Bulldozers and builder-driven construction** is implemented in `res://scenes/builder_assault.tscn`, **before power generation**, and **accepted for continued feature development with a documented interface exception**. Collection and the bounded control-group compatibility continuation are finished; criterion 7 remains **NOT VERIFIED**. The [M12 report](milestone-12.md) saves the owner decision, all eight technical acceptance statuses, commands, source correspondence, failed executions, rendered evidence and launch/controls. The existing identifier is retained without renumbering historical milestones. This heading retains its historical link anchor for earlier reports. Headquarters-based automatic construction in the earlier scenes remains explicitly a legacy prototype.

- Headquarters produce **Bulldozer units** through the existing production and safe-deployment systems.
- Selecting an owned Bulldozer exposes available building choices, including Supply Depot. Valid placement creates an ordinary paid unfinished site using existing placement, payment, geometry and navigation systems.
- The assigned Bulldozer must travel by ordinary movement to a valid working position and actively construct. Construction progresses only while an eligible assigned owned Bulldozer is working at that site.
- Moving, stopping, losing or otherwise invalidating the builder pauses construction. Another owned Bulldozer can be assigned to resume the unfinished site without purchasing the site again.
- Bulldozers construct buildings. Supply Depots produce Supply Trucks/Collectors, which gather and deliver supplies and do not construct buildings.
- Extend the existing construction, production, harvesting, payment, placement, navigation and lifecycle systems. Preserve paid-site accounting, cancellation/refunds, bounded navigation updates, destruction, callbacks, match freeze and Restart; do not replace these systems.

The focused acceptance covers HQ-produced builders, exact-one builder-selected placement, real approach and physical work eligibility, progress gating, Move/Stop/death pause, a normally produced replacement's resumption, and depot/collector separation in the real playable economy-to-army flow. Bulldozer defaults are 500 credits / six seconds, 200 HP and speed 3.5. Existing building prices and working durations remain unchanged. The 1000-credit opening funds a 300-credit Depot and its first 200-credit Collector. No power system or enemy builder strategy is part of M12; M13 adds the separate power extension below.

The original M12 wrapper finished all 68 tests with 65 passing, 8814 checks, 14 failures and exit 1. Builder mechanics and earned integration pass; the ledger remains **1000 initial + 400 real deposits − 1350 paid = 50**. The original graphical Attack Move failure used **group 3**, after staging and five individual viewport-selection checks. Its first composite assertion failed before accepted Attack Move dispatch. The earliest missing observation is the complete accumulated selection before Ctrl+3; original stored IDs and restored selection are also absent. No production defect or invalid original fixture assumption was established.

The bounded continuation first replayed all original UI predecessors on current source (97 graphical checks pass), then added failure-only stage snapshots and focused compatibility assertions. Final source-matched coverage passes **1,030 checks across six executions**: Attack Move UI **113/116**, groups **82/82**, builder **314/323**, headless/graphical. It covers modifier release, consumed keys, visible recall, builder/Help/placement-to-army transitions, Collector/combat group membership, combat-only Q, X Stop and result/Restart. Paid Rifle and Rocket still independently engage and physically resume travel. The [M12 report](milestone-12.md#attack-move-ui-finding-and-bounded-follow-up) records the unchanged original input contract, later 94/97-test differences, exact evidence limits, commands and snapshots. These passes do not establish repair of the original occurrence; criterion 7 remains **NOT VERIFIED**. The owner separately accepts continued feature development under the documented interface exception.

Final evidence reuses 62 unchanged original matrix entries and six focused entries: 66 passing out of 68, 8882 checks and seven separately deferred movement failures. The original headless stress failures (`choke_30`/`L_shape_30`, unit 27) and graphical stress failures (`choke_50`, unit 6) retain unknown attribution. Runtime, scenes, resources, tools and unrelated tests are unchanged. No full matrix rerun, movement investigation, engine upgrade, commit or human playtest occurred. Final source correspondence, documentation links/anchors and whitespace outcomes are saved with the report.

## Next feature — Milestone 13 power generation

**Milestone 13 — Power Plants and power management is COMPLETE (2026-09-09)** in the directly launchable `res://scenes/power_assault.tscn`; open it in Godot and press F6. The [M13 report](milestone-13.md) records all eight PASS acceptance groups, the one completed fresh-copy matrix (74/74 executions, 9,711 checks, zero failures/native errors, exit 0), source correspondence, every earlier failure and reused screenshots at 1280x720 and 1920x1080 with Help expanded/collapsed. Its six power suites pass 829 checks within that matrix. The reported job-continuity failure compared successive jobs across legitimate completion; corrected fixture assertions verify the original deployment exactly once and retained progress/rate on the same still-active successor. Production implementation, costs and durations were unchanged. Documentation links and whitespace checks pass. This heading retains its existing link anchor.

Bulldozers construct Power Plants for 500 credits and 10 actual working seconds. Each completed living eligible plant generates 10; Barracks require 2 and Factories 4 even when idle or exit-blocked. Below demand, every eligible owner Barracks/Factory trains at 50%; sufficient power restores 100% without resetting jobs. HQs, Depots and units consume zero. Power is owner-wide and scoped to the current match; ordinary destruction, cancellation, recovery and Restart apply.

The player retains the 1000-credit builder opening with no prebuilt plant or production structures. The enemy adds one explicit normally registered completed plant, without wallet or wave-timer changes and without rebuilding AI. A compact local HUD shows generation/demand and shortages; selected producers show current rates and plants use P on the minimap. Earlier scenes remain opted out. The historical M12 interface exception and separately deferred movement findings remain open. The M14 extension is recorded below.

## Milestone 14 — completed ground defense extension

Open `scenes/defense_assault.tscn` in installed Godot 4.7.2 and press **F6**, or launch with `--path . res://scenes/defense_assault.tscn`. The original player HQ, Bulldozer, three Rifles, 1000 credits and finite supplies remain, without a prebuilt player battery or generator. The new stationary battery costs 700 credits and 12 actual builder working seconds, has 600 HP, consumes 3 power, and uses existing hitscan for 18 damage every 0.75 seconds within 12 units. A 0.25-second scan interval, 3-radian/second turret and 8-degree facing tolerance bound its ground-only defense. Preview/unfinished sites add no operational demand; power loss clears targets and blocks new shots while preserving cooldown and full nominal demand. Existing production slowdown, commands, construction and lifecycle systems remain authoritative.

The enemy's one explicit battery guards a limited western central approach; eastern/northern access to its existing plant remains available. No enemy construction/rebuilding, wave-timer changes, aircraft or air-defense work is added. The normal 90/60-second schedule is verified separately from the labelled 600-second economic test instance. The earned integration reconciles **1000 + 1500 deposits − 1700 spending = 800 credits**, proves six actual defensive hits and a subsequent ordinary Rifle attack on the paid plant interrupting firing, then verifies real-weapon HQ victory and viewport Restart in a separate labelled combat fixture.

The [completed report](milestone-14.md) preserves the original smoke failures and supported fixture/runtime corrections, all 11 saved phases, final source correspondence, exact commands and exits, eight reused visual inspections and 18 normal UI captures at 1280×720 and 1920×1080. The one final matrix completed **82/82 tests, 81 passing, 10,751 checks, five failures and exit 1**; all **1,040 defense checks** and affected gameplay/UI suites pass. The sole failed execution is graphical movement stress: unit 7, `choke_50` arrival/traversal/settling and `cluster_50` arrival/settling. Attribution remains **UNKNOWN**; it is preserved without retry or historical investigation. No newly introduced regression is established, and no full movement acceptance is claimed. M12's accepted interface exception, its NOT VERIFIED criterion 7 and all deferred movement findings remain unchanged. Final link/source/whitespace checks pass. No human playtest occurred, and no subsequent milestone is begun.

## Milestone 15 — completed air combat extension

**Milestone 15: COMPLETE (2026-09-09).** Open [Air Assault](../scenes/air_assault.tscn) in installed Godot 4.7.2 and press **F6**, or launch from the repository root with `--path . res://scenes/air_assault.tscn`. The original main scene remains F5. The [completed report](milestone-15.md) records all eight original acceptance statuses: **PASS** for builder/paid production; takeoff/flight/deployment; targeting separation; helicopter/AA combat; power/destruction/callbacks/freeze/Restart; mixed commands/groups/minimap/interface; earned integration; and earlier-system preservation/accurate reporting. No required M15 feature remains outstanding.

The unchanged 1000-credit HQ/Bulldozer/Rifle opening uses finite harvesting to fund an Airfield (1000 credits, 15 builder work seconds, 3 power), a plant and a helicopter (600 credits, 10 training seconds, doubled under shortage). The aircraft waits for launch clearance, physically climbs to altitude 8, flies at bounded speed above ground obstacles and fires ground-only guided rockets. The builder-created AA battery costs 800 credits, takes 12 work seconds and demands 3 power; it aims at and hits hostile aircraft only, with shortage blocking new shots and preserving cooldown. The normal enemy retains paid ground production and its 90/60-second waves, plus one explicit starting AA and helicopter with a single 120-second sortie. No hidden funds/power, replacement aircraft or air planner are added.

The existing `m15-final-matrix-01` completed **92/92 executions: 46 headless + 46 graphical, 11,756 checks, zero failures/native error lines, aggregate exit 0**. Fresh import also passes; all 93 wrapper records have exit 0. The ten air executions contribute **1,005 checks already included in the matrix**. Both earned integrations pass and reconcile **1000 + 2000 deposited − 2600 spent = 400**: actual paid takeoff, rocket damage to the enemy ground battery, powered AA killing that aircraft, then an ordinary Rifle attacking the enemy plant. The explicit earned fixture delays enemy waves/sortie to 600 seconds; normal scene values are unchanged.

The report preserves every failure from all 11 phases and supported corrections: malformed-weapon atomic batch rejection, safe deleted-object callbacks, typed WeakRefs, equivalent-phase flight measurements and 720p full-queue spacing. All **354 source files matched** before closeout; final runtime, resources, scenes, tests and tools still match, with only the three handoff documents changed. The [source/link/whitespace record](../validation-output/m15/final-handoff-checks.json) and [documentation whitespace record](../validation-output/m15/documentation-whitespace-checks.json) pass. [Exact screenshot paths](milestone-15.md#screenshots-and-visual-handoff) preserve 1280×720 and 1920×1080 layouts, Help, selection, production, markers and result/Restart; ten existing current-source inspections are reused. No engine test or screenshot review was repeated during closeout, and no human playtest occurred.

Landing, rearming, fuel, fixed-wing aircraft and air-to-air combat are intentionally excluded. Flight supports a flat datum, clear cruise plane and bounded local spacing for small aircraft populations; general aerial obstacle routing/congestion, runway/taxiing, crash damage and enemy air production remain outside scope.

**Historical interface exception:** M12's original group-3 occurrence stays OPEN / UNKNOWN and criterion 7 NOT VERIFIED. **Deferred movement findings:** their status remains UNRESOLVED, including M14's preserved graphical unit-7 failure; current passing movement checks do not close historical failures. No historical investigation, engine upgrade, commit or Milestone 16 work was started.

## Milestone 16 — walls, gates and single-barrier breaching

<!-- M16_HANDOFF_START -->
**Milestone 16: accepted for continued feature development with deferred movement validation.** This applies the existing priority decision above, separately from technical acceptance. The original `m16-final-matrix-01` completed **102/102 executions, 99 passing, 12,829 checks, five assertion failures, three native error lines and aggregate exit 1**. All ten M16 executions pass **1,073 checks already included**. Headless/graphical construction failed one preview prerequisite each; graphical movement stress failed three `choke_50` assertions. Each native-error line is the harness's `push_error` report of one of those movement assertions; saved stacks establish no separate engine/script/navigation error. A bounded reproduction established that the preview fixture sampled normal camera smoothing before a current advisory approval. Its test-only correction waits for natural settling and strengthens fresh occupancy rejection without changing placement rules. Source-matched targeted results separately pass **271 headless + 274 graphical = 545 checks**, no failures/native errors, aggregate exit 0. No demonstrated M16 feature regression remains unaddressed in the saved evidence. The original matrix remains failed; all original failures and the diagnostic failure remain preserved. Graphical movement attribution, including any relationship to the navigation correction, is **UNKNOWN**, so acceptance group **7 remains NOT VERIFIED**; groups **1–6 and 8 PASS**. The [saved M16 report](milestone-16.md) records exact error classifications, source correspondence and retained evidence. This scheduling acceptance does not claim movement repair or full movement acceptance.
<!-- M16_HANDOFF_END -->

Open [Fortified Assault](../scenes/fortified_assault.tscn) in installed Godot 4.7.2 and press **F6**, or use `--path . res://scenes/fortified_assault.tscn` from the repository root. Its original HQ/Bulldozer/three-Rifle/1000-credit opening has no free player perimeter. Wall costs 100 credits / 4 actual builder work seconds / 400 HP; Gate costs 250 / 8 / 700 HP. Select the builder, choose a piece, align ends on the one-unit grid and use **R** or the orientation button for 0°/90°. Owned completed gates expose **Open Gate / Close Gate**; an occupied or newly occupied pending closure stays open with **Gate obstructed**, without displacement, damage or later automatic closure. Both owners can pass an open doorway, while supports remain physical and the full span remains reserved against construction.

Final feature evidence verifies exact joins, connected navigation polygons without degenerate strips, distinct authored boundaries, pending departure/deletion, exact X/Z preview snapping with authoritative ground-height validation, real earned Collector traversal and normal weapon destruction. The unchanged 90-second paid wave spends 300 for three Rifles, destroys the real 700-HP gate and physically crosses after navigation-ready resumption. Both earned integrations conserve **1000 + 600 deposited - 1050 spent = 550**. No warnings, movement requirements, footprints or engine versions were changed during collection.

Single-piece placement, manual gates, no repairs and no general multilayer siege planner remain deliberate limits. The separate protected-access placement complaint is not verified fixed by the synthetic preview case and is outside the movement deferral. The accepted M12 interface exception remains OPEN/UNKNOWN with historical criterion 7 NOT VERIFIED; the [M16 movement finding](movement-issue-records.md#m16--graphical-choke-50-movement-validation-open) and earlier deferred movement findings remain unresolved. This documentation-only reconciliation performs no gameplay/test edits, Godot execution, matrix rerun, additional screenshot inspection, movement investigation, engine upgrade, commit or tag. No human playtest occurred.

**Next planned feature: the buildable superweapon facility.** It is not implemented during this closeout.

## Baseline and validation provenance

The working tree was clean at the start of the original priority update, at commit `d5fc444`. At that baseline, `scripts/rts_unit.gd` SHA-256 was `2f46d67b3267ab233405457f8c84e83ebc46b8f461e75deef46ee5c3f33b34d0`, matching final source in the [latest gate repair report](milestone-5-gate-repair.md). Current M12 source correspondence is recorded in the [M12 handoff](milestone-12.md#reused-evidence-and-final-tested-source-correspondence).

That report records a final 36-execution integration matrix with 4,740 assertions and zero failures, while preserving an earlier graphical unit-23 gate run with 39 checks and three failures on the repaired code. Historical unit 41 and original Issue B remain unresolved. These are prior results and do not establish full movement acceptance.

The M7 completion pass started at clean existing implementation commit `643099f`. Its previously unfinished report now records **225 headless / 230 graphical** passing Vehicle Production checks and **124 / 130** passing Base Assault checks. The first M7 complete matrix passed 43 of 44 test executions, with three headless projection-fixture assertions failing; attribution to historical movement issues or M7 integration remains unknown. That failed execution is retained in [movement issue records](movement-issue-records.md) and [the M7 report](milestone-7.md), separately from passing required feature behavior. No gameplay, movement, existing test, engine or dependency changes were made during this completion pass.

The M7 completion pass then ran one complete matrix on a freshly imported source copy: **43/44 test executions passed, 5555 assertions and 2 failures**, with all M7 and M6 feature checks passing in both modes. Graphical `choke_50` arrival/settling failed for unit 5; its relationship to earlier failures remains unknown. This failed execution was retained without retry or movement investigation. [Exact commands, per-suite results and screenshots](milestone-7.md#completion-verification-on-the-installed-engine) preserve the M7 handoff. Feature A–G acceptance passed in that verification; full movement/Milestone 5 acceptance remains unpassed. That pass contained no M8 implementation.

A repeated M7 implementation request was audited against the existing `643099f` implementation with those four documentation edits preserved. Its single current-source matrix, `request-audit-20260907`, passed fresh import and **44/44 test executions, 5555 assertions and zero failures**. M7 passed **225 headless / 230 graphical** checks and M6 **124 / 130**. No gameplay or test change was needed. [That request's verification](milestone-7.md#repeated-request-verification) retains separate source hashes, commands, results and images; it does not close the earlier failed movement executions or reopen investigation. Only handoff documentation was updated during that completed M7 pass.

M8 began on clean commit `7f9a1e4d17f6f030061bc9b729484ebab225fa08`. Its starting audit matched all **226** files in the completed M7 request snapshot except the three expected handoff documents (`README.md`, `docs/roadmap.md`, `docs/milestone-7.md`). Gameplay, scenes, resources, tests and tools matched, so the recorded M7 results are reused as the baseline without another historical validation campaign. The [M8 report](milestone-8.md#baseline-and-preservation) retains the comparison and separates newly executed M8 results from that baseline. The installed Godot version and dependencies remain unchanged.
