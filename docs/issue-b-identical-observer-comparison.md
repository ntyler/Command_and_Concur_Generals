# Milestone 5.0.1 — identical-observer production comparison

2026-09-07. **Comparison: DISCRIMINATING.** The first observed separation is unit33's actual avoidance callback at frame200, while the compared preceding state, requests and cached paths still agree. Its differing completed movements follow already-different callback inputs. This does not support a demonstrated projection-displacement explanation for that early split. The reason for the native callback difference remains unknown. **Unit41's captured stall and original Issue B remain unresolved; Milestone5 remains blocked.**

This is the one authorized diagnostic pair following the [early-divergence audit](issue-b-early-divergence-audit.md). No production repair, acceptance change, new recorder/framework, archive, commit, engine change or human keyboard-and-mouse test occurred. All six authorized engine invocations are consumed; no further run follows.

## Frozen identities and narrow adaptations

The active project's existing 203 source files and uncommitted changes were preserved. Both isolated projects were copied from that source. The control's movement file was then copied from the authenticated pre-projection snapshot; the active project's production was never replaced.

| Identity | Control | Experiment |
|---|---|---|
| Isolated project | `D:/GitHub/Command_and_Concur_m501l_control_20260907T191249Z` | `D:/GitHub/Command_and_Concur_m501l_experiment_20260907T191249Z` |
| Movement SHA-256 | `9b26194e6a0ef505573b0f51a8a7cdb1c409fae24c9526ccae0284cef29bbe2b` | `1af671d1fd8da9f65b89a7e95022e0785c94b05885b7d8ed3f79f7fbf17e4da4` |
| Observer/runner | Identical adapted implementation | Identical adapted implementation |
| Fixture | Frozen 50-unit `d56be378…55307` | Identical |
| Engine | Godot `4.7.2.stable.official.ed1daf0bf`, binary `c8f0a6bc…846643` | Identical |

All 51 production/resource/configuration files were checked against the retained pre-projection source. Only `rts_unit.gd` differs: the audited 11-line addition. Both versions include Issue A. The 97-entry producer manifests differ only in that file and explicit `comparison-arm.json` identity metadata. Project configuration, geometry, all fixtures, probe/field/recorder code, storage policy and parameters match. Existing global-class/UID caches were copied identically; no extra import was run. Each invocation retains its exact arguments and complete source snapshot.

Only three copied files were adapted, identically in both arms:

- Runner: validate the selected arm's exact hash and case prefix from identity metadata; require matching prepared inputs; use that identity in the capture header.
- Launcher: include arm metadata in producer verification; use fresh per-arm names/slots and only `parser-1`; preserve all original successful-preparation and independent-capture gates.
- Bridge: verify selected identity against metadata, receipt, input and actual source; use fresh case/slot paths and exclusive entry for every invocation.

No arm-dependent gameplay or recording branch was added. The original wrapper, gameplay body, waits, dispatch order, assertions and teardown remain unchanged. Copied text line endings were normalized; execution copies match byte-for-byte between arms. Exact edits/hashes are in `validation-output/m501l-adapted/adaptation.patch`, `adaptation-notes.md` and `m501l-installed-review.json`.

## Complete execution ledger

Every process ran serially with the unchanged external wrapper. Both arms' parser and precluster gates passed before either moving run. Fixed60, the 75-second route, 90-second command, 180-second internal watchdog and 240-second external timeout remained unchanged.

| Case | Native checks / failures | Exit | Capture |
|---|---:|---:|---|
| Control parser-1 | Not applicable | 0 | Parser only |
| Control validate-1 | 19 / 0 | 0 | Complete; zero cluster commands/movement calls |
| Experiment parser-1 | Not applicable | 0 | Parser only |
| Experiment validate-1 | 19 / 0 | 0 | Complete; zero cluster commands/movement calls |
| Control observe-1 | **33 / 2** | **1** | Complete and independently valid |
| Experiment observe-1 | **33 / 0** | **0** | Complete and independently valid |

Total: **six engine invocations, two moving comparisons, 104 native assertions, two failures**. The control failed the unchanged sampled speed/attempt assertion and actual-call step assertion. It retains **34 actual step excesses**, maximum **.129939133**, against **.0843333333**. This remains an ordinary failed run. Its valid capture and unchanged source allowed the already-prepared experiment to proceed. The experiment has zero excesses, maximum **.083334958**. Both runs have one returned, advancing-stamp observation for each actual movement and no slide contacts.

Both runs preserved all accepted goals/orders, all50 arrival, navigation/obstacle and full180-tick settling/separation predicates. Control/experiment settled minimum separation is **1.235341668 / 1.395535707**, with zero stationary displacement. Arrival does not waive the control's step failures. The validator retains181 complete frame rows after first all-arrived observation because the original sampler begins on the following observation boundary; the sampler still executes exactly180 ticks.

The [execution review](../validation-output/m501l-execution-review.json) records all outcomes, exact commands, sources, exits and capture identities. Both preparation captures contain189 frames and9,450 idle callbacks. Moving control contains1,159 frames,57,950 callbacks and23,247 actual movement calls; experiment contains1,373 frames,68,650 callbacks and25,206 calls. Both have zero capture errors/incomplete delegates. The experiment's additional214 frames remain retained.

## First observed separation

The streaming comparison aligns **physics frame, stable unit ID, public order, operation phase and within-unit callback occurrence**. Global sequence numbers below are references, not alignment keys. Analysis uses float32-exact comparison and componentwise absolute tolerance **1e-6**; both locate the same first difference. Neither changes acceptance thresholds. The comparator passed a234-frame same-J self-comparison; an independent extraction confirmed the boundary.

All100 public command results, all50 initial cached paths and all50 initial scalar path-query results agree under those comparisons. Parking occurs at frame6; cluster dispatch remains frame189 in ID order1–50. Compared movement observations agree through frame199. At frame200, all50 preceding physics positions, requests and cached paths still agree; callback delivery order is1–50 in both runs.

The first difference is **unit33, order2, callback occurrence0**. In each capture its frame record is **seq976**, preceding physics observation **30609**, callback admission/completion **30691/30692**, motion receipt **9983**. The common cached path has14 points and index2, last changed at **seq962/frame196**; current waypoint is `(11.0054893494,0,7.1500000954)`.

| Actual boundary | Control | Experiment |
|---|---|---|
| Start position | `(10.0042695999,0,2.2286319733)` | Same |
| Recorded requested velocity | `(.9967976809,0,4.8996315002)` | Same |
| **Actual callback input** | **`(1.1793365479,0,4.6967010498)`** | **`(1.2665268183,0,4.6442742348)`** |
| Returned endpoint | `(10.0239248276,0,2.3069102764)` | `(10.0253782272,0,2.3060364723)` |
| Actual displacement | `.080708244423` | `.080231107565` |
| Stamp / delta / contacts | `199→200`; `1/60`; empty | Same |

Endpoint separation is **.001695849097**. Both endpoints match their respective **offline float32** `start + desired_input * delta` proposals. All550 preceding movement endpoints through frame199, and all2,250 endpoints across frames189–233 in **each arm**, likewise match their own proposals. There is no demonstrated displacement correction explaining this prefix. Equal endpoints do not prove an inactive repaired branch or absence of its additional native path query.

Unit35 also differs at frame200; unit46 follows at202, peer49 at224, and unit41 at225. Unit41's first unequal callbacks are **seq1029/frame225**, admission/completion34485/34486. At233,21 before-physics positions differ; unit41 is **.007007265** apart. The first changed waypoint/cache index occurs later, unit29/frame241. First endpoint/proposal deviations occur at control49/frame246 and experiment49/frame247; these residuals alone do not identify an internal projection branch.

This is interpretation **B** from the authorization: callback separation precedes a qualifying displacement difference. Matching recorded requests and body states do **not** establish identical native solver inputs, selected neighbors, constraints or synchronization. Internal map/update identity and branch execution remain unavailable in the inherited schema. No engine timing, threading or generic nondeterminism diagnosis is made. Identical observer implementation removes one deliberately varied factor, without proving equivalent native state between processes or explaining the old E execution.

## Unit41 mechanism and disposition

Both arms accept unit41's exact `(28.5,0,17.5)` goal on order2 at **seq857/frame189**. Neither exercises its historical zero-displacement/retry stall:

| Observation | Control | Experiment |
|---|---|---|
| Peer49 ARRIVED result | seq2502/frame526 | seq2997/frame655 |
| Unit41 ARRIVED result | seq2818/frame596 | seq2951/frame638 |
| Unit41 returned movement calls | 407 | 449 |
| Unit41 zero-displacement calls / recoveries | 0 / 0 | 0 / 0 |

The control has70 observed frames with moving41 and already-arrived49, yet no zero-displacement recovery mechanism. The experiment has no such interval. Thus parked-peer precedence alone is insufficient. These are new derived-cluster executions, omitting the earlier30-unit/gate trajectories; they neither reconstruct the historical E approach nor supply original Issue B unit4's missing chronology. No focused stall regression or candidate repair is established.

Moving captures and sibling invocation/source/log/result files:

- [Control capture](D:/GitHub/Command_and_Concur_m501l_control_20260907T191249Z/validation-output/m501l-control-observe-1.jsonl), **225,116,985 bytes**, SHA-256 `f0d9756d27608ffd8435073a258ee95515dbc9fe97aaa9009fa45f3d0b5b007e`.
- [Experiment capture](D:/GitHub/Command_and_Concur_m501l_experiment_20260907T191249Z/validation-output/m501l-experiment-observe-1.jsonl), **265,469,651 bytes**, SHA-256 `2fe78a1f8b9bd688e13da45ff792b7229a891ca2e695452b3f661e37d72c7621`.
- [Comparison results](../validation-output/m501l-compare.json), [script](../validation-output/m501l-compare.py), [independent boundary extraction](../validation-output/m501l-independent-boundaries.json). Source/context/phase references and derived calculations are retained.

Offline preparation/display mistakes are recorded in the adaptation notes and `m501l-analysis-notes.md`; none consumed an extra engine invocation. Final source/ledger verification and `git diff --check` are recorded in `m501l-final-verification.json`. All existing working files and closed execution ledgers remain unchanged; only this new report and ignored comparison artifacts were added.

- Comparison: **DISCRIMINATING** about the first observed separation; historical cause remains unknown.
- Issue A: **PRESERVED; no new runtime regression-suite claim**.
- Projection correction: **PRESERVED**.
- Unit41 boundary stall: **UNRESOLVED**.
- Original Issue B: **UNRESOLVED**.
- Milestone5: **STILL BLOCKED**.

Both moving slots are closed. No repair, waiver, additional batch or next milestone follows.
