# Issue B: bounded hypothesis selection (written before engine experiments)

Source audit and offline analysis of all six traces precede this plan. Only H1 is selected. No production correction is justified.

| Item | H1: repeated final-goal fallback under persistent local closure |
| --- | --- |
| Mechanism | `_recover` charges one attempt before searching. With no selected detour it reinstalls the final goal. Without >=0.3 final-route progress, expiry resumes the same blocked route; later eligible samples spend further attempts, then the cap fails the order. |
| Evidence it can explain | Original directly recorded FAILED/attempts=8 before the 90-second command deadline. Four later actual final-goal fallbacks establish that this admission path occurs, including full graphical 1's goal holder. |
| Inconsistent or limiting observations | All four later fallbacks succeed; none of the six cluster traces expires or fails. Original route was intended to be reachable. A closed synthesized enclosure is an intentionally bounded crowd outcome, not evidence of a defect. |
| Missing historical event | Repeated no-detour selections and continuing closure BEFORE original failure. Final parked neighbors may have arrived AFTER it. Earlier expenditure across successful episodes also remains possible. |
| Expected signature | One increment per admission; actual target equals accepted final goal; no meaningful progress; expiry; unchanged order and static map; next admission respects cooldown; after eight trials a later insufficient-progress sample calls FAILED before command/test deadlines. |
| Synthesized experiment | Historical M5 complete source copy. Unchanged stress obstacle mesh; 50 normal registered units. Actor ID4 initially at printed B stop (31.41587,0,19.78682), one public move to (33,0,22). Ten normally parked peers on radius1.4 ring, 36-degree spacing, 9-degree offset from the initial goal direction; 39 distant spectators. Ring chord approximately0.86525 exceeds body diameter0.86 while gaps are below combined moving/parked avoidance clearance. No private state writes, altered defaults, or teleportation after creation. Hold all neighbors. |
| Control | Identical initial source/geometry/population/actor command. After first naturally observed recovery admission, command the goal-facing ring neighbor outward to radius4 through ordinary `move_to`. It remains registered and avoidance-enabled. Expected actual arrival and 180-tick settling; retained counter demonstrates that clearance, not a budget refund, permits progress. |
| Capture | Test-only subclass calls original super methods. Wrap actual final-goal path query return without a second query; record admission/clear/terminal before and after; bounded 120-tick request/callback/applied-displacement buffer; all peer state transitions; existing diagnostic candidate probes explicitly recomputed after admission. |
| Deadline and stop | One graphical experiment and one graphical control, serial wrapper240s, inherited harness180s, route75 simulated seconds, unchanged command90s. Record setup/signature failures. No parameter search or reruns to obtain desired result. Diagnostic expectations are separate from original acceptance. |
| Interpretation | Matched signatures establish a possible accounting sequence and distinguish it from double charging or completed-detour cycling in this setup. They do not connect H1 to original B. If no fallback occurs, report that contradiction rather than relabel other detours as fallback. |

The observed retention of attempts after meaningful progress is already source- and trace-supported. An eight-obstruction course would manufacture a long unrecorded history; it is not selected. No stale-callback, navigation-reset, or third hypothesis has a supporting runtime signature in the six traces.
